/* @flow */

function setRemote (host) {
  // fs.open('~/.react-native-remote/remote')
  console.log(`This script will eventually set ${host} as the remote destination.`)
}

module.exports = setRemote
